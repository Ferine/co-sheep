{
  "name": "co-sheep",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:tauri": "npm run clean:bundle-temp && npm run build",
    "bundle:dmg": "bash ./scripts/build-dmg.sh",
    "clean:bundle-temp": "rm -f src-tauri/target/release/bundle/macos/rw.*.dmg src-tauri/target/release/bundle/dmg/rw.*.dmg",
    "preview": "vite preview",
    "tauri": "bash ./scripts/tauri-wrapper.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
